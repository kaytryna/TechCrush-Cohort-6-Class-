//While loops
let tries = 0;
while (tries < 3){
    console.log(tries + 1); // arithmetic operator
    tries++; // increment
}


let count = 10;
while (count > 0){
    console.log(count);
    count--; // decrement; assignment operator
}